import React from 'react';
import {withRouter} from "react-router-dom";

const Catalog = () => {
    return (
        <div>
            <h1>Catalog</h1>
        </div>
    );
};

export default withRouter(Catalog);