import React from 'react';
import {withRouter} from "react-router-dom";

const UploadImage = () => {
    return (
        <div>
            <h1>Upload</h1>
        </div>
    );
};

export default withRouter(UploadImage);